VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# GitHub Pages: add the same two values as repository Secrets (Settings → Secrets → Actions)
#   VITE_SUPABASE_URL
#   VITE_SUPABASE_ANON_KEY
# Use Project URL + anon public key (or publishable key) from Supabase → Settings → API
